{
  "name": "myplace-site",
  "private": true,
  "version": "1.0.0",
  "description": "Sitio institucional MYPLACE — home bilingüe (es/en), Atelier y Portfolio generados estáticamente.",
  "scripts": {
    "build:i18n": "node build/build-i18n.js",
    "build:atelier": "node scripts/build-atelier.mjs",
    "build:portfolio": "node scripts/build-portfolio.mjs",
    "build": "npm run build:i18n && npm run build:atelier && npm run build:portfolio"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}
